# 复制为 .env（不要提交）。

# 【订阅登录用户】：无需任何 key。
#   你已用 `claude` 直接登录，后端调 claude -p 会自动用登录态。
#   ⚠ 不要设 ANTHROPIC_API_KEY —— 一旦设了会改走 API 按量计费，而非订阅。

# 【仅当你想用 API key 而非登录时才设】：
# ANTHROPIC_API_KEY=sk-ant-xxx

# 【若调试一直回退 mock（后端找不到 claude）】：显式指定 claude 路径
#   先在终端跑 `which claude` 拿到路径，填到这里；或启动时 CLAUDE_BIN=$(which claude) uvicorn ...
# CLAUDE_BIN=/Users/you/.nvm/versions/node/vXX/bin/claude

# 上云用阿里云时再加（本地阶段不需要）：
# ALIBABA_CLOUD_ACCESS_KEY_ID=
# ALIBABA_CLOUD_ACCESS_KEY_SECRET=
# DASHSCOPE_API_KEY=
