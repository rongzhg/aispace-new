fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
httpx==0.27.2
python-multipart==0.0.20  # 技能市场上传技能包（multipart/form-data）
# 注：mcp_server.py（通用 agent 的平台工具）已改为纯标准库实现，不再依赖 mcp 包，
# 任意 python3（含 3.8/3.9）即可运行，无需额外安装。
