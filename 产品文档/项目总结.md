# AISpace · Agent 平台 — 项目总结

> 截至 2026-06-25。本文件是项目的进展与决策快照，供团队/AI 快速了解全貌。

## 1. 目标

在集团内复制一个「Claude Managed Agent」平台：让用户在平台上创建、配置、调试、版本化管理 AI Agent。配套两个工作目标——团队 AI native 化（全栈 + vibe coding）、需求 T+1 快速交付。

## 2. 工作方法（已确立）

采用 **spec-driven development**：PM 用自然语言 + demo 表达意图，沉淀成统一格式的 spec，全栈开发（的 AI）据 spec + demo 实现，尽量减少口头沟通。关键是先建「共享契约」作为多人/多 AI 协作的地基。

推进顺序：**契约 → 用真实项目（本平台）打磨契约 + 沉淀 demo → 后续再做需求 agent**。

## 3. 已产出（目录结构）

文档分两类：**产品文档/**（开发 vibe coding 主要参考）与 **技术文档/**（工程规范与架构）。

### 产品文档/（做什么）
- `specs/`（A–K）— 功能需求 + EARS 验收标准。详见 `specs/README.md`
  - A 生命周期 · B 框架 · C 模型 · D 工具 · E 技能 · F 版本 · G 预览/导出 · H 权限 · I 运行/发布 · J 基础设施 · K 项目空间
- `glossary.md` — 统一术语表
- `spec-template.md` — spec 写作规范（每条 SHALL 对应一个测试）
- `视觉规范.dc.html` — 权威色板/排版/组件样式
- `templates/` — 配置文件模板（Claude Code 的 `claude.md`；OpenClaw 的 `role/agent/user.md`）
- `设计参考-index-demo.html` — 视觉参考稿
- `项目总结.md` — 本文件

### 技术文档/（怎么做）
- `前端工程规范.md` — React+TS+AntD 组件与设计 token、目录约定
- `完成定义-DoD.md` — Definition of Done（接口契约、分层测试、CI 关卡）
- `接口契约.md` — 后端 API 速览（FastAPI，已实现）
- `架构设计.md` — 后端/运行模式架构（短任务/长会话；云端 sandbox 方向）

### demo/（可运行原型，前端）
`demo/index.html` React+AntD（CDN）。**自动探测后端**：连得上 `localhost:8000` 走真实 API，否则回退内存 mock。`app.jsx` 为源码，esbuild 编译内联进 `index.html`。

### backend/（已落地，Python）
FastAPI + SQLite。Agent CRUD、版本、空间/成员、发布、调试/试跑（无头调本机 Claude Code）。见 `../技术文档/接口契约.md`。

## 4. 关键产品决策（已拍板）

- **框架**：Claude Code（单 `claude.md`）、OpenClaw（`role/agent/user.md` 三文件 Tab）可选；Custom/Hermes 占位禁用。prompt 落点：Claude Code → claude.md；OpenClaw → role.md（人设）+ agent.md（行为）。
- **模型**：所有框架必选模型；按供应商分组（Anthropic / OpenAI / DashScope / AISpace-伏渊 如 yufeng-vl）；清单全平台统一、无权限控制、由接口下发。参数为数据驱动动态表单，定义来自外部接口，本平台不维护。
- **版本**：编辑保存即生成新版本（N+1），无变更禁用保存；历史版本只读，详情页可切换查看任意版本快照；支持**任意两版本对比**（字段级 + 配置文件逐行 diff）。回滚 = 基于历史版本生成新版本（后续）。
- **草稿**：本期**不做**，留待后续版本（demo 已移除）。
- **权限**：粒度 = 项目空间 + 角色 + 资产；无权限的工具/技能锁定置灰不可选。
- **组织**：项目空间（Workspace）为权限边界，资产按空间隔离；新用户自带默认空间；可创建/切换空间、管理成员；一个空间可多 Owner（至少一名）。三角色：平台管理员 / 项目管理员(空间 Owner) / 项目成员。
- **登录**：生产对接集团 SSO；demo 免登录 mock 用户。审计：方案已记录，本期不做。
- **创建/编辑交互**：**双栏 Builder**（左配置：框架/基本/模型/文件/工具技能；右可收起面板：调试 + 配置预览），不分步。
- **调试/试跑**：Builder 右栏「调试」与详情页「试跑」复用同一对话组件——Builder 喂草稿配置，详情喂已保存版本。

## 5. 视觉风格（已定）

开发者工具风、浅色高密度、克制。主色 = 墨色 `#17171C`（主按钮），强调 = 靛蓝 `#4F46E5`（仅链接/选中）。1px 发丝描边、6–8px 圆角、控件高度 34、系统字体、模型名/参数用等宽。语义色仅用于状态。详见 `视觉规范.dc.html`。

## 6. demo 覆盖现状

| 模块 | demo + 后端状态 |
|---|---|
| A 列表（行卡片，含发布状态）/详情/创建/编辑/软删 | ✅ 前端+后端 |
| B 框架卡片/差异化配置文件/模板预填 | ✅ |
| C 模型分组选择 + 参数 | ✅ |
| F 版本快照/切换/对比 diff；编辑与发布按规则升版本 | ✅ 前端+后端 |
| H 工具技能锁定态（mock 权限） | ✅ |
| I 调试/试跑（真实运行，多轮续接，引擎不可用回退占位）| ✅ 前端+后端 |
| I 发布（选运行环境/隔离级别，自动升版本，运行成可对话服务）| ✅ 前端+后端 |
| I 框架可插拔：同一发布/服务/对话流程支撑多框架（Claude Code、OpenClaw）| ✅ 前端+后端 |
| I **OpenClaw 多 Agent 部署**：同时跑多个、按 Agent 路由、互不影响、未配引擎回退占位 | ✅ 已验证 |
| C OpenClaw 每 Agent 不同模型 | ✅ 已验证 |
| I Playground（与所有已发布 Agent 真实对话）| ✅ 前端+后端 |
| J/K 空间切换/创建/成员管理（多 Owner） | ✅ 前端+后端 |
| 云端发布/灰度、审计、RBAC 后台、工具/技能注册 | ⬜ 后续 |

## 7. 待办 / 待确认

- ✅ 已解：OpenClaw 三文件（role/agent/user.md）语义已定——角色 / 行为 / 用户上下文（见 Spec B）；与具体引擎的映射等实现细节见 技术文档/
- 各供应商真实模型清单（伏渊系列补全）
- 配置预览结构化 schema（影响后端契约）
- 后端接口契约（OpenAPI）、分层测试落地（按 ../技术文档/完成定义-DoD.md）
- 后续版本：草稿、版本回滚、发布/灰度、运行日志、权限分配后台、自定义工具/技能注册

## 8. 进展日志（截至 2026-06-26）

- 视觉按 `视觉规范.dc.html` 重做：墨色主按钮 + 靛蓝强调、行卡片列表、双栏 Builder。
- 文档拆分为 `产品文档/`（开发 vibe coding 主读）与 `技术文档/`。
- **落地 Python 后端**（FastAPI + SQLite）：CRUD/版本/空间成员/发布/调试。
- **前后端打通**：前端自动探测后端，真实读写。
- **调试/试跑改为真实运行**：后端无头调本机 Claude Code（`--append-system-prompt` 注入配置、`--resume` 多轮），失败显示真实报错。
- **发布**：先选运行环境（共享/独立/即用即弃），把配置物化成 Claude Code 工作目录（`CLAUDE.md`，demo 落本机），返回运行命令；编辑页发布会按"有改动则升版本"规则自动升版本。
- **Playground**：与所有已发布 Agent 真实对话。
- 列表显示发布状态（已发布 vN / 未发布）。
- 环境坑记录：需装 Claude Code CLI（`npm i -g @anthropic-ai/claude-code`）；后端找不到 claude 时用 `CLAUDE_BIN` 指定；订阅登录用户勿设 `ANTHROPIC_API_KEY`；公司网络若做 TLS 拦截会导致 claude 连不上 Anthropic（换网络或走百炼/伏渊备选）。
- **发布即起服务**：点发布为该 Agent 启动常驻服务（每 Agent 一进程一端口），暴露 `/chat`、`/chat/stream`；本地单实例（换版本停旧起新，服务记录带 version）。
- **API 三协议**：协议1 一次性 JSON、协议2 SSE 流式（token 级增量，`--include-partial-messages`）、协议3 异步作业（后续）；demo 实现 1+2。Playground 走 SSE 逐字流式。
- **运行服务运维**（复用列表/详情，不单独建页）：列表徽标运行中/已停止、详情「运行服务」区（状态/URL/API 示例/停服/启动）、侧栏「停止所有 Agent 服务」(pkill 兜底清孤儿)。
- **Chat 统一入口**：默认通用 agent + slash 选内置 skill（`/agent-creator`、`/skill-creator`），通用 agent 轻量真执行平台操作。
- **通用 agent 正式路径**：Claude Code + 平台工具（`backend/mcp_server.py` 的 MCP），订阅登录无需 API key（`templates/general-agent/`）。
- 创建项目空间限平台管理员。

## 9. 下一步建议

- 落实后端 OpenAPI 契约与分层测试（按 `../技术文档/完成定义-DoD.md`、`接口契约.md`）。
- 正式环境：本机运行 → **云端每用户 sandbox**（见架构设计的待决：多 agent、依赖隔离）。
- 用本 demo + spec 跑通一个真实需求的完整链路，沉淀「黄金样板」模块。
